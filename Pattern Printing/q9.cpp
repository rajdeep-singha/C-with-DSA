#include<iostream>
using namespace std;
int main(){
int no_of_chars =9;
// No of char in one line
    for(int i=0;i<=no_of_chars;i++){
      char ch = (char)('A'+i);
      cout<<(char)('A'+i);
    }
    return 0;
}
